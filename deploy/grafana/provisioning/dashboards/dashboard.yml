apiVersion: 1
providers:
  - name: api-tester
    orgId: 1
    folder: api-tester
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
